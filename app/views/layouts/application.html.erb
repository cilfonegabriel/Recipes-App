<!DOCTYPE html>
<html>
  <head>
    <title>Recipes</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'partials/navbar' %>

    <main class="container mx-auto mt-10 px-5 flex flex-col items-center justify-center">
      <%= yield %>
      <p class="text-red-500 font-extrabold"><%= alert %></p>
      <p class="text-blue-500 font-extrabold"><%= notice %></p>
    </main>
  </body>
</html>
